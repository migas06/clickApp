---
description: Gera uma checklist de qualidade de requisitos ("unit tests para inglês") para a feature ativa com base num domínio ou pedido do utilizador.
---

## Argumentos

```
$ARGUMENTS
```

## Instruções

Lê e segue **na íntegra** as instruções definidas em `.claude/commands/speckit.checklist.md`.

Substitui `$ARGUMENTS` pelo conteúdo acima (e.g. o domínio da checklist: "ux", "security", "api", etc.).
