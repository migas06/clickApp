---
description: Cria ou atualiza a especificação de uma feature a partir de uma descrição em linguagem natural.
---

## Argumentos

```
$ARGUMENTS
```

## Instruções

Lê e segue **na íntegra** as instruções definidas em `.claude/commands/speckit.specify.md`.

Substitui `$ARGUMENTS` pelo conteúdo acima (a descrição da feature que o utilizador escreveu após `/specify.specify`).
